import boundary from '../assets/boundary.jpg';
import logo from '../assets/logo.png';
import splash from '../assets/splash.jpg';

interface ImageFiles {
  boundary: any;
  logo: any;
  splash: any;
}

const Images: ImageFiles = { boundary, logo, splash };

export default Images;
